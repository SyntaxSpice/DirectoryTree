import { Routes } from '@angular/router';
import { FolderTreePageComponent } from './features/folder-tree/pages/folder-tree-page.component';

export const routes: Routes = [
  { path: '', component: FolderTreePageComponent },
];
